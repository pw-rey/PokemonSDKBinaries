--- a/src/MovieImpl.cpp
+++ b/src/MovieImpl.cpp
@@ -471,8 +471,6 @@
     
     const sf::Texture& MovieImpl::getCurrentImage() const
     {
-        static sf::Texture emptyTexture;
-        
         if (m_videoSprite.getTexture())
         {
             return * m_videoSprite.getTexture();
@@ -480,7 +478,12 @@
         }
         else
         {
-            return emptyTexture;
+            // Destroy the fallback with its movie, before Windows DLL detach.
+            // A static Texture keeps an OpenGL context alive into driver teardown.
+            if (!m_emptyTexture)
+                m_emptyTexture.reset(new sf::Texture());
+
+            return *m_emptyTexture;
         }
     }
     
--- a/src/MovieImpl.hpp
+++ b/src/MovieImpl.hpp
@@ -152,6 +152,7 @@
         std::shared_ptr<Demuxer> m_demuxer;
         std::shared_ptr<Timer> m_timer;
         sf::Sprite m_videoSprite;
+        mutable std::unique_ptr<sf::Texture> m_emptyTexture;
         std::list<sf::Sprite> m_subtitleSprites;
         Streams m_audioStreamsDesc;
         Streams m_videoStreamsDesc;
